import PendingImplementation from '@/components/PendingImplementation';

export default function VenueDetailsPage() {
    return (
        <PendingImplementation
            featureName="Détails du lieu"
            description="Consultez bientôt les statistiques détaillées, l'historique des matchs et les classements spécifiques à ce lieu."
        />
    );
}
